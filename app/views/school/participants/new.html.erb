<% content_for :title, "New participant" %>

<div class="d-flex align-items-center">
  <h1>New participant</h1>
  <%= link_to "Back", participants_path, class: "btn btn-sm btn-dark ms-auto" %>
</div>

<%= render "form", participant: @participant %>
