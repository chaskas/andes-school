<h1 class="text-center mb-4">Editing Participant</h1>

<%= render "form", participant: @participant %>

<div class="d-flex">
 <%= button_to " Show", @participant, class: "btn btn-warning bi bi-eye me-1", method: :get %>
  <%= button_to " Return", participants_path, class: "btn btn-dark bi bi-arrow-return-left me-1", method: :get %>
</div>
